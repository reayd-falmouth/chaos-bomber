name: Linting

on:
  pull_request:
    branches: [ main, master ]

jobs:

  black:
    name: Black
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3.1.0
